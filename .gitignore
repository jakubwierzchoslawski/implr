# OS
.DS_Store
Thumbs.db
# editor
.idea/
.vscode/
*.swp
# build artefacts
*.skill
*.zip
# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
# setuptools build output
build/
dist/
# Claude Code session state
.claude/settings.local.json
# generated by the bootstrap; plugin/agents is the source
.claude/agents/
.claude/skills/
# install.sh also provisions a target workspace. implr does not consume its own
# pipeline, so that output is bootstrap noise here: docs/implr/schemas and
# templates are byte-identical copies of plugin/, and committing them would
# recreate the second source of truth that removing scaffold/ deleted.
docs/implr/
/CLAUDE.md
