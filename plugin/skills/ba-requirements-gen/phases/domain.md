# Phase: domain (inline envelope — v3.1)

> **This file is superseded.** As of v3.1, the orchestrator builds a complete `domain_envelope`
> inline and dispatches `requirements-domain-worker` directly from `SKILL.md` Phase 3.
> `requirements-domain-worker` no longer reads `requirement-schema.md`, `implr.config.yaml`,
> or `DEV-STANDARDS.md`. See `skills/ba-requirements-gen/SKILL.md` Phase 3 for the current
> dispatch contract and `domain_envelope` shape.
